# Contributing to Mail Jack

Thanks for your interest in contributing!

### Run locally
1. Clone the repo  
   `git clone https://github.com/KaranHotwani/mail-jack.git`
2. Copy `.env.example` → `.env` and update credentials.
3. Run the service:  
   `go run ./cmd`

### Pull Requests
- Use clear commit messages.
- Describe what you changed and why.
- Make sure `go build` passes before submitting.

Let's make email simple together 🚀
